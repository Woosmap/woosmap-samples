# Python Script to export data from a Woosmap Customer and import theme to another customer

You need to define parameters for public_key (origin data) and private_key (destination data)
sample usage:

    python geojson_to_woosmap.py

